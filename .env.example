# ── Obligatorias ────────────────────────────────────────────────────────────
POSTGRES_PASSWORD=cambia_esto
OPENAI_API_KEY=sk-...

# ── Opcionales (valores por defecto mostrados) ───────────────────────────────
POSTGRES_USER=tacrc
POSTGRES_DB=tacrc

EMBED_MODEL=text-embedding-3-small
EMBED_DIM=1536
LLM_MODEL=gpt-4o-mini

# Directorio local con los PDFs descargados
PDF_HOST_DIR=./pdfs
# Directorio local con el JSON de metadatos exportado por la extensión Chrome
JSON_HOST_DIR=./data

# Contraseña para el acceso al frontend (análisis de pliegos)
APP_PASSWORD=tacrc2024

# Nivel de log de la API (debug, info, warning, error)
LOG_LEVEL=info
